import 'package:flutter/material.dart';

class Defaults {
  static final Color bottonNavItemColor = Colors.blueGrey;
  static final Color bottomNavItemSelectedColor = Colors.blue;

  static final bottomNavItemText = [
    'Inbox',
    'Starred',
    'Sent',
    'Drafts',
    'Trash',
    'Spam',
  ];

  static final bottomNavItemIcon = [
    Icons.inbox,
    Icons.star,
    Icons.send,
    Icons.mail,
    Icons.delete,
    Icons.warning_rounded,
  ];
}
